#include <stdio.h>

int main() {
    float score = 90.5;
    printf("a: %f\n",score);

    return 0;
}
// โค้ตทำงานได้ปกติเพราะ 90.5 เป็น Double เเละใส่ %f เพื่อบอกว่าเป็น float
   เพราะว่าfloatใช้สำหรับค่าทศนิยม และint ใช่กับการแสดงผลจำนวนเต็ม
